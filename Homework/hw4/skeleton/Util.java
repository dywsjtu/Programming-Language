public class Util {
  /**
   *  Finds the index of the maximum element in the given 1D array.
   *  The elements must be Comparable to each other, and the array
   *  must have at least one element.
   */
  public static <T> int indexOfMax1D(Array<T> array) {
    assert array.rank() == 1 : "rank must be 1";
    assert array.size() >= 1 : "must have at least 1 element";
    return 0; // replace with your code
  }
}
